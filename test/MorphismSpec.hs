module MorphismSpec (spec) where

import Test.Hspec
import Test.QuickCheck
import Control.Exception (evaluate)
import AlgebraicStructures.Base
import AlgebraicStructures.Morphisms

spec :: Spec
spec = do
  describe "stub" $ do
    it "stub" $ do
      True `shouldBe` True
